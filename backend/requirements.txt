# FehlerJagd Backend.
#
# KERN (immer nötig – reicht für FJ_SCORING=stub, also bis zur Phase-0-Freigabe):
fastapi
uvicorn[standard]
python-multipart      # Audio-Upload (multipart/form-data)
sqlmodel              # SQLite-ORM
pyyaml                # Story-Graph (graph.yaml) laden
python-dotenv         # .env / Provider-Konfiguration

# ECHTES lokales Scoring (nur für FJ_SCORING=local; nutzt phase0/fjp0).
# torch/torchaudio ggf. über den CPU-Index installieren:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch
torchaudio
soundfile             # WAV laden/resamplen
numpy
